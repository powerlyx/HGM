# Core
numpy==2.2.6
scikit-learn==1.7.2
torch==2.13.0
transformers==5.14.1
sentence-transformers==5.6.0
nltk==3.10.0
rank-bm25==0.2.2

# LLM API clients
openai==2.46.0
python-dotenv==1.2.2
tiktoken==0.13.0

# Evaluation metrics
rouge-score==0.1.2

# Optional（默认关闭或未接入主链路）：
# bert-score==0.3.13   # 需 AMEM_ENABLE_BERT_SCORE=1 才计算
# ollama               # 仅在使用 --llm-backend ollama 时需要（requests 直连，无强制依赖）
